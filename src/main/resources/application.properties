# Server Configuration
server.port=4110
spring.application.name=splitbill

# Database Configuration (In-Memory H2)
spring.datasource.url=jdbc:h2:mem:splitbilldb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# JSON Serialization Configuration (snake_case)
spring.jackson.property-naming-strategy=SNAKE_CASE

# Application Personalization
app.github.username=harycp
